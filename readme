# Running the Code
1. Run 'make'
2. Make sure to have two instances on two different servers open
3. Run './sendfile' on one instance and type 's' to start as a server
4. Run './sendfile' on another instance and type 'c' to start as a client